// Iterators allows you to iterate through the collection of items.
// Consumer does not need to care how the items stored whether it is an array, object, set or map
// Using the iteratos next() method will allow the client to move to next item in a list.

// Below code not working in node
// let numbers = [1, 2, 3, 4];
// let iterator = numbers.values();
// let sum = 0;
// let next = iterator.next();
// while(!next.done) {
//     sum += next.value;
//     next = iterator.next();
// };
// console.log(sum);
  